# Flask Secret Keys - Generate using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=generate_with_secrets_token_hex
JWT_SECRET_KEY=generate_with_secrets_token_hex

# Database Configuration
DB_HOST=localhost
DB_USER=your_database_username
DB_PASSWORD=your_database_password
DB_NAME=ciphervault
DB_PORT=3306

# Optional: Remote Database (if applicable)
REMOTE_DB_HOST=your_remote_host
REMOTE_DB_USER=your_remote_username
REMOTE_DB_PASSWORD=your_remote_password
REMOTE_DB_NAME=your_remote_database
REMOTE_DB_PORT=3306
